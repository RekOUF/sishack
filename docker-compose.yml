version: "3.8"

services:
  sishack:
    build: .
    container_name: sishack
    restart: unless-stopped
    network_mode: host
    environment:
      - SISELI_ACCOUNT=rvrhee@gmail.com
      - SISELI_PASSWORD=***REMOVED***
      - HA_URL=http://192.168.177.87:8123
      - HA_TOKEN=your_ha_long_lived_token
      - INTERVAL_MINUTES=5
